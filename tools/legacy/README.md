# Legacy tools

`build_dashboard.py` is **obsolete**. It targeted the old 7D/30D/90D revision windows and markdown-only dashboard flow.

Use `tools/export_web_data.py` + the `web/` SPA instead. Prefer `tools/publish_github_pages.sh` to publish to `kumahsu1118-ui/ai-eps-monitor`.
